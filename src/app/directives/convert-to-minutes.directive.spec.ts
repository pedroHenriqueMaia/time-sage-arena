import { ConvertToMinutesDirective } from './convert-to-minutes.directive';

describe('ConvertToMinutesDirective', () => {
  it('should create an instance', () => {
    const directive = new ConvertToMinutesDirective();
    expect(directive).toBeTruthy();
  });
});
